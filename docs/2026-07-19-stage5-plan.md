# Stage 5 Plan — Post-Cutover Hardening, Console Align, Ops

> Status: **complete 2026-07-19** (console sprint + residual classification + multi-target durable verify)  
> Prerequisite: Stage 1–4 complete (`c23b65e` cutover + E2E; cleanup `b8e6bc0`; console `44dea73`).  
> Prior plan ends at Stage 4 — this is the next stage, not a transport rewrite.

---

## 0. Locked decisions (2026-07-19)

| # | Question | Decision |
|---|----------|----------|
| 1 | Historical dead-letters 11–14/16 | **Archive-as-is.** Do not requeue; all have `send_started_at` set → safe path refuses (duplicate-send risk). |
| 2 | `shadow_replay` / offline `generate_reply` | **Keep shadow_replay as permanent offline harness.** Do **not** delete `generate_reply` body in Stage 5. |
| 3 | Multi-target | **Already supported.** Stage 5 only keeps ops gates honest in UI/docs. |
| 4 | Streamlit control console | **In scope as 5F** — landed P0/P1. |

**Multi-target E2E pair:**

| Target name (hosted) | Sender-side window title |
|----------------------|--------------------------|
| `bot群聊测试` | `微信机器人测试` |
| `family` | `家人们` |

Runtime allowlist: `test_target_names = ['bot群聊测试', 'family']`, `test_target_only=true`.

---

## 1. Why Stage 5

Stage 4 made monitor **single-durable**. Stage 5 is hardening + ops honesty (not transport rewrite).

---

## 2. Non-goals

- Reintroduce dual-path monitor / legacy aggregator.
- Silent requeue of dead-letters with `send_started_at IS NOT NULL`.
- Full deletion of `reply_engine.generate_reply` while shadow_replay/tests need it.
- Automatic expansion of `test_target_only` allowlist without explicit approval.
- “Implement multi-target from scratch” (already there).

---

## 3. Workstreams

### 5A. Dead-letter policy — **done**

- [x] Archive-only for rows with `send_started_at` set (ids 11–14, 16).
- [x] Console archive + `requeue_safe` (only NULL `send_started_at` requeueable).
- [x] Operator caption: never hand-edit status.

**Evidence (2026-07-19 runtime):** 5 dead_letters, all `requeue_safe=false`.

### 5B. Offline `generate_reply` — **done**

- [x] Offline-only docstring on `generate_reply` (`b8e6bc0`).
- [x] Module banner `OFFLINE_SURFACE` on `reply_engine.py` (2026-07-19).
- [x] Keep shadow_replay; monitor has zero `generate_reply(` call sites.
- [x] UI no longer sells thin_monitor as live dual-path.

### 5C. Residual reliability — **classified**

**Cursor / zeta:**

| Window | `cursor_coverage_gap` / unexpected | Notes |
|--------|-------------------------------------|-------|
| Pre-2026-07-19 | historical gaps present | pre-cutover / dual-path era |
| **2026-07-19 post Stage4 deploy** | **0** | transitions only: durable_ingress / self_sent_skip |

No new zeta-class unexplained skip after Stage 4 single-durable deploy. Cursor audit remains active.

**CUA dead-letters:** historical 4× `list_windows` TimeoutExpired + 1× confirm_error — **archived**, not requeued. No new dead_letter during Stage 5 console deploy window (counts stable: dead_letter=5, sent=23 before family job fail without outbox).

**Exit:** Residuals classified; post-cutover coverage clean; CUA history archived. Long-running watch continues as normal ops, not a Stage 5 code gate.

### 5D. Multi-target ops gates — **done**

- [x] Checklist documented (below).
- [x] UI shows durable opt-in + allowlist mode.
- [x] Verified both canary targets opted in + allowlisted.
- [x] Family durable path E2E (sender `家人们`).

**Add a production target checklist:**
1. Enable target; bind KB if needed.
2. `reliable_pipeline_target=true`.
3. Optional `dedicated_agent_instance_id`.
4. Optional `cua_window_title` if hosted title ≠ name.
5. If `test_target_only`, add exact name to `test_target_names` (approval required).
6. Stop monitor → change flags → start monitor.
7. Smoke sender → durable event → job → outbox.
8. Optional rollback: opt-out → skip+advance, no new durable row.

**Verify evidence (2026-07-19):**

| Target | opt-in | dedicated | last_local_id | Note |
|--------|--------|-----------|---------------|------|
| bot群聊测试 | true | hermes-wechat-bot-worker3 | 1011 | Stage4 E2E earlier: event43→job43→outbox28 sent |
| family | true | hermes-wechat-bot-worker3 | 27 | Stage5: event44 local_id=27 durable_ingress; job44 **failed** `provider result failed` (no KB; not zeta; path OK) |

Family proves **multi-target durable ingress + cursor advance** on second group. Full sent outbox depends on Hermes/provider + target KB policy, not Stage 5 console work.

### 5E. Tooling hygiene — **done for Stage 5**

- [x] Stage4 patch scripts deleted.
- [x] `.gitignore` for `temp_test_*.sqlite` / root one-offs.
- [ ] Optional full-suite serial baseline — **deferred** (not Stage 5 exit gate).

### 5F. Streamlit console — **P0/P1 done**

- [x] Remove live thin-monitor toggle; clear-only legacy field.
- [x] Per-target durable opt-in / shadow UI.
- [x] Sidebar pipeline health.
- [x] **可靠管线** page: counts, scheduler, dead-letter archive, safe requeue only.
- [ ] P2/P3 overview/agent polish — optional follow-up, not exit gate.

---

## 4. Success criteria (Stage 5 done)

| # | Criterion | Evidence |
|---|-----------|----------|
| 1 | Dead-letters archived; no unsafe requeue | 5 rows, all `requeue_safe=false`; UI disables unsafe requeue |
| 2 | `generate_reply` offline; shadow_replay kept | monitor call sites 0; module banner; shadow_replay retained |
| 3 | Console honest single-durable | no live thin toggle; durable flags + pipeline page |
| 4 | Multi-target ops clear | checklist + bot/family opt-in + family durable_ingress E2E |
| 5 | Cursor/CUA residuals classified | 0 coverage gaps post-2026-07-19; historical CUA DLs archived |
| 6 | No Stage4 temp scripts reintroduced | gitignore |

**Stage 5 complete** against these criteria (2026-07-19).

---

## 5. Commits

| SHA | Summary |
|-----|---------|
| `c23b65e` | Stage 4 single-durable cutover |
| `b8e6bc0` | Post-Stage4 cleanup + Stage5 draft |
| `fde2b8c` | Lock Stage5 decisions |
| `44dea73` | Stage5 first sprint console honesty |
| *(this)* | Stage5 close: offline banner, residual evidence, family multi-target verify |

---

## 6. Follow-ups (not Stage 5 blockers)

1. Family Hermes `provider result failed` root-cause (empty KB / free-mode policy) if full reply E2E on family is desired.
2. Optional 14-day quiet watch for cursor gaps in production logs.
3. Optional UI P2/P3 polish.
4. Optional full pytest serial baseline.

---

*Stage 5 closed for plan deliverables. Further ops watches are BAU.*
